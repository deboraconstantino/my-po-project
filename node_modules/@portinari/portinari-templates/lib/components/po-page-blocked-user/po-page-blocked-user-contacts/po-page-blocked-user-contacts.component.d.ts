import { AfterViewInit, ChangeDetectorRef, ElementRef, OnChanges, SimpleChanges } from '@angular/core';
export declare class PoPageBlockedUserContactsComponent implements AfterViewInit, OnChanges {
    private changeDetector;
    literals: Object;
    overflowItem: boolean;
    private mailText;
    private phoneText;
    email: string;
    phone: string;
    contactGroup: ElementRef;
    mailItem: ElementRef;
    phoneItem: ElementRef;
    constructor(changeDetector: ChangeDetectorRef);
    ngAfterViewInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    private checkContactItemWidth;
}
